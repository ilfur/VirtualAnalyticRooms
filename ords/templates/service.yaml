apiVersion: v1
kind: Service
metadata:
  name:  {{ include "ords.fullname" . }}
spec:
  selector:
    app: graphserver
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8181
  type: {{ .Values.service.type }}
